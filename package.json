{
  "name": "feature-flags",
  "private": true,
  "packageManager": "pnpm@11.17.0",
  "scripts": {
    "dev:api": "pnpm --filter @ff/api run dev",
    "dev:web": "pnpm --filter @ff/web run dev",

    "build": "pnpm --filter @ff/domain run build && pnpm --filter @ff/db run build && pnpm --filter @ff/api run build && pnpm --filter @ff/web run build",
    "typecheck": "tsc --noEmit -p packages/domain/tsconfig.json && tsc --noEmit -p packages/db/tsconfig.json && tsc --noEmit -p apps/api/tsconfig.json",
    "test": "pnpm --filter @ff/domain run test && pnpm --filter @ff/api run test",
    "db:generate": "pnpm --filter @ff/db run generate",
    "db:migrate": "pnpm --filter @ff/db run migrate"
  },
  "devDependencies": {
    "typescript": "^5.8.3"
  },
  "engines": {
    "node": ">=20"
  }
}
